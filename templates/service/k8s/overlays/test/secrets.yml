apiVersion: v1
kind: Secret
metadata:
  name: {{projectName}}
data:
  PGADMINUSER: {{{pgAdminUserTest}}}
  PGADMINPASSWORD: {{{pgAdminPasswordTest}}}
  PGPASSWORD: {{{testDbPassword}}}
  MQPASSWORD: {{{mqPasswordTest}}}
